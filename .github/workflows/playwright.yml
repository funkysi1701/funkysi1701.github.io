name: Playwright E2E (production)

permissions:
  contents: read

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  playwright:
    uses: ./.github/workflows/reusable-playwright.yml
    with:
      base_url: https://www.funkysi1701.com
    secrets: inherit
