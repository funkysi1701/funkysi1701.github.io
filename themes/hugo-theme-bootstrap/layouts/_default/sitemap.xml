{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{ $exclude := slice "tags" "month" "categories" "year" }}
  {{ range .Data.Pages }}
    {{ if not (in $exclude .Data.Plural) }}
      <url>
        <loc>{{ .Permalink }}</loc>
        {{ with .Lastmod }}
          <lastmod>{{ .Format "2006-01-02T15:04:05-07:00" }}</lastmod>
        {{ end }}
        <priority>{{ if .IsHome }}1.0{{ else if .IsPage }}0.8{{ else }}0.5{{ end }}</priority>
      </url>
    {{ end }}
  {{ end }}
</urlset>
