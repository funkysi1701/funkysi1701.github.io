# Blog CI/CD has moved to GitHub Actions — see .github/workflows/
# Disable this pipeline in Azure DevOps after GHA parity is verified (task 4.3).

trigger: none
pr: none

pool: Docker

steps:
  - checkout: self
  - script: |
      echo "No Azure Pipelines steps — blog deploy, E2E, and Pa11y run in GitHub Actions."
    displayName: "Retired blog pipeline placeholder"
