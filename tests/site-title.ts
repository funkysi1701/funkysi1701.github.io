/**
 * Hugo `title` in config varies by environment:
 * - production (`config/_default`): Funky Si's Blog
 * - blog-dev (`config/development`): Funky Si's Deve
 * - blog-test (`config/staging`): Funky Si's Test
 */
export const SITE_TITLE_PATTERN = /Funky Si/i;
